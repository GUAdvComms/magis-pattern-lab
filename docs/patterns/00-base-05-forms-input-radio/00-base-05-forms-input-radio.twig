<div class="form-check">
    <input type="radio"
      class="form-check-input"
      name="{{ namevalue|default('gender') }}"
      id="{{ namevalue|default('gender') }}"
      value="{{ value|default('male') }}" checked>
    <label class="form-check-label" for="exampleRadios1">
        First radio
    </label>
</div>
<div class="form-check">
    <input type="radio"
        class="form-check-input"
        name="{{ namevalue|default('gender') }}"
        id="{{ namevalue|default('gender') }}"
        value="{{ value|default('male') }}">
    <label class="form-check-label" for="exampleRadios1">
        Second radio
    </label>
</div>

